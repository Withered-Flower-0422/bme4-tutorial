---
sidebar_position: 5
---

# 光源

## API

### `SetLightIntensity(ID: int, intensity: float) -> None`

设置光源强度。

- `intensity`：强度

### `SetLightColor(ID: int, RGB: vec3) -> None`

设置光源颜色（不透明度恒为 `1`）。

- `RGB`：颜色，各项范围为 `0 <= x <= 1`
