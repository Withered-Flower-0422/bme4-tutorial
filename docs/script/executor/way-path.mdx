---
sidebar_position: 7
---

# 路径

## API

### `WayPathStart(ID: int) -> bool`

启动路径。

- `returns`：是否启动成功

### `WayPathStop(ID: int) -> bool`

停止路径。

- `returns`：是否停止成功

### `WayPathPause(ID: int) -> bool`

暂停路径。

- `returns`：是否暂停成功

### `WayPathResume(ID: int) -> bool`

恢复路径。

- `returns`：是否恢复成功
